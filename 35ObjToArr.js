// convert object to array and vice versa

// const obj ={
//     name:"manoj",
//     age:22,
//      city:"Kolhapur",
// };

// // convert the object to an array of key-value pairs.

//  let entries = Object.entries(obj);
//  console.log(entries);
//  console.log(entries.flat ()); 
 
//  // convert the array of key-value pairs back a obj
 
//  let newObj = Object.fromEntries(entries);
//  console.log(newObj);
 

const Obj ={
    name :"manoj",
 lastName :"patil",
 age:22,
 city:"pune",
}
 let entries = Object.entries(Obj);
console.log(entries);
 console.log(entries.flat());
 